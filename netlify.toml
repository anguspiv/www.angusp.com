[build]
  command = "npm run build && rm -rf .next"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  CYPRESS_INSTALL_BINARY = "0"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

[[redirects]]
  from = "/about"
  to = "/"
  status = 301
  force = true
